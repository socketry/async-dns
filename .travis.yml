language: ruby
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y bind9
rvm:
  - 2.2
  - 2.1
  - 2.0
  - rbx-2
  - ruby-head
  - jruby-head
matrix:
  allow_failures:
    - rvm: rbx-2 
    - rvm: ruby-head
    - rvm: jruby-head
env: COVERAGE=true
notifications:
  irc: "irc.freenode.org#celluloid"